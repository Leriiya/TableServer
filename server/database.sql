CREATE DATABASE perntable;

CREATE TABLE mytable(
    table_id SERIAL PRIMARY KEY,
    description VARCHAR(25)
    quantity VARCHAR(25),
    price_one VARCHAR(25),
    price_lot VARCHAR(25),
    volume VARCHAR(25),
    company VARCHAR(25),
    phone VARCHAR(25)
);